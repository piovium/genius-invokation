Require stdio language-server evidence and bound work-tree Git enumeration

Adds the gts-lsp gate to the GTS assignment: the delivery surface itself
must be proven over stdio against @gi-tcg/gts-language-server with TNB
identity, not implied by the editor session. The reassignment binds the
active GTS task, its seal and its task-file hash in a reviewed scope
transition, and from.gates stays a subset of to.gates.

Fixes tnb task dispatch: hashing the pinned submodules enumerates ~140k
files, and `git ls-files --others` alone measures 87.9 s in typescript-go,
so the single 30 s Git bound failed deterministically. Index queries keep
30 s; work-tree enumeration gets a separate 300 s bound. Both stay bounded
and no gate timeout or acceptance threshold changes.

Seal 2.3.0 f4699ff16446db19b715ffdf9ab1a759b9b4c6f7cdfb4bd27b3539a97b534e52
passes its own 134/134 sealed selftests.